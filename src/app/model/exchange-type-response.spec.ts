import { ExchangeTypeResponse } from './exchange-type-response';

describe('ExchangeTypeResponse', () => {
  it('should create an instance', () => {
    expect(new ExchangeTypeResponse()).toBeTruthy();
  });
});

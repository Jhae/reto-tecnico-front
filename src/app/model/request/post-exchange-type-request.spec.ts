import { PostExchangeTypeRequest } from './post-exchange-type-request';

describe('PostExchangeTypeRequest', () => {
  it('should create an instance', () => {
    expect(new PostExchangeTypeRequest()).toBeTruthy();
  });
});
